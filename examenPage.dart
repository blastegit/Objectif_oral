import 'package:flutter/material.dart';

Widget examenPage(BuildContext context) {
  return Container(
    color: Colors.yellow,
    alignment: Alignment.center,
    child: const Text('Page des examen'),
  );
}